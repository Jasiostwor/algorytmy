def bfs(graph):
    """Przechodzenie wszerz (BFS) - do zaimplementowania."""
    if graph.n == 0: return "Graf jest pusty."
    return "[TODO] Przechodzenie wszerz (BFS) nie zostało jeszcze zaimplementowane."

def dfs(graph):
    """Przechodzenie w głąb (DFS) - do zaimplementowania."""
    if graph.n == 0: return "Graf jest pusty."
    return "[TODO] Przechodzenie w głąb (DFS) nie zostało jeszcze zaimplementowane."