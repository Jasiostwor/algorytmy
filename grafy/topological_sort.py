def tarjan_sort(graph):
    """Algorytm Tarjana - do zaimplementowania."""
    if graph.n == 0: return "Graf jest pusty."
    return "[TODO] Sortowanie topologiczne Tarjana nie zostało jeszcze zaimplementowane."

def kahn_sort(graph):
    """Algorytm Kahna - do zaimplementowania."""
    if graph.n == 0: return "Graf jest pusty."
    return "[TODO] Sortowanie topologiczne Kahna nie zostało jeszcze zaimplementowane."